  var exec = require('child_process').exec
    , dirname = '/tmp/' // change to the dir you expect to have something during the test
    ;

  exec('find ' + dirname, function (error, stdout, stderr) {
    console.log(error, stdout, stderr);
  });
